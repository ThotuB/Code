#include <stdio.h>
#include <stdlib.h>

typedef struct _dllist {
    int key;
    struct _dllist *prev, *next;
} dllist;

typedef struct _llist {
    int key;
    struct _llist *next;
} llist;

int main() {
}
